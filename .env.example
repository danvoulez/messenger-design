# Authentication Configuration
# IMPORTANT: Change these values in production!

# JWT Secret Key - Use a strong random key in production
# Generate with: openssl rand -base64 32
JWT_SECRET=ubl-messenger-secret-key-change-in-production

# WebAuthn Configuration
# RP (Relying Party) Name - displayed to users during authentication
RP_NAME=UBL Messenger

# RP ID - Your domain name (without protocol or port)
# For local development: localhost
# For production: yourdomain.com
RP_ID=localhost

# Origin - Full URL where the app is hosted
# For local development: http://localhost:3000
# For production: https://yourdomain.com
ORIGIN=http://localhost:3000

# Server Configuration
NODE_ENV=development
PORT=3000
